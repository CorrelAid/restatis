# check_function_input

check_function_input

## Usage

``` r
check_function_input(
  code = NULL,
  term = NULL,
  sortcriterion = NULL,
  category = NULL,
  detailed = NULL,
  type = NULL,
  date = NULL,
  similarity = NULL,
  error.ignore = NULL,
  ordering = NULL,
  pagelength = NULL,
  database = NULL,
  area = NULL,
  caller = NULL,
  verbose = NULL,
  raw = NULL
)
```

## Arguments

- code:

  Parameter to be checked

- term:

  Parameter to be checked

- sortcriterion:

  Parameter to be checked

- category:

  Parameter to be checked

- detailed:

  Parameter to be checked

- type:

  Parameter to be checked

- date:

  Parameter to be checked

- similarity:

  Parameter to be checked

- error.ignore:

  Parameter to be checked

- ordering:

  Parameter to be checked

- pagelength:

  Parameter to be checked

- database:

  Parameter to be checked

- area:

  Parameter to be checked

- caller:

  Parameter to be checked

- verbose:

  Parameter to be checked

- raw:

  Parameter to be checked
